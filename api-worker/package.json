{
  "name": "80kg-sprint-api-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config vitest.config.mjs",
    "typecheck": "tsc --noEmit",
    "dev": "npx wrangler@4 dev --config wrangler.toml",
    "deploy": "npx wrangler@4 deploy --config wrangler.toml"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "4.1.12"
  }
}
